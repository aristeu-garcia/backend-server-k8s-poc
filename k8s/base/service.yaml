apiVersion: v1
kind: Service
metadata:
  name: backend-server
  labels:
    app: backend-server
spec:
  type: NodePort
  selector:
    app: backend-server
  ports:
    - name: http
      port: 80
      targetPort: 3000
    - name: metrics
      port: 3001
      targetPort: 3001
