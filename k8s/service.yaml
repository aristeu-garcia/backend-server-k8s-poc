apiVersion: v1
kind: Service
metadata:
  name: backend-server
  namespace: backend-poc
  labels:
    app: backend-server
spec:
  type: NodePort
  selector:
    app: backend-server
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
