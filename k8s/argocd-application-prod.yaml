apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-server-prod
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/aristeu-garcia/backend-server-k8s-poc.git
    targetRevision: main
    path: k8s/overlays/prod

  destination:
    server: https://kubernetes.default.svc
    namespace: backend-poc-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true

  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: backend-server
      namespace: backend-poc-prod
      jsonPointers:
        - /spec/replicas
